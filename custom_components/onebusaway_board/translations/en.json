{
  "config": {
    "step": {
      "user": {
        "title": "OneBusAway Board",
        "description": "Add one stop. Repeat this for each stop you want a board for.",
        "data": {
          "name": "Name",
          "url": "API base URL",
          "key": "API key",
          "stop": "Stop ID",
          "scan_interval": "Poll interval (minutes)"
        }
      }
    },
    "error": {
      "auth": "Invalid API key.",
      "cannot_connect": "Failed to connect to the OneBusAway API.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "already_configured": "This stop is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "OneBusAway Board options",
        "data": {
          "scan_interval": "Poll interval (minutes)"
        }
      }
    }
  }
}
